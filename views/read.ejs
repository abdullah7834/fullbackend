<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Read User</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900  p-4 text-white">
        <div class="nav mb-10">
<a  class="text-blue-500" href="/">Back to home</a>
        </div>  
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
        <div class="users flex gap-4 flex-wrap">
            <% if(users.length > 0){%>

          <% users.forEach(function(user){ %>
            <div class="user max-w-72 p-10 bg-zinc-800 rounded-lg">
                <div class="w-full h-72 bg-zinc-700 rounded-lg overflow-hidden">
                    <img class="w-full h-full rounded-lg object-top" src="<%= user.imageurl%>" alt="">
                </div>
                <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
              <div class="flex gap-3 mt-10">
              <a class="text-zinc-300" href="/edit/<%=user._id%>">Edit User</a>
              <a  class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
      </div>
                 </div>
           <%})%>
           <%} else {%>
            <h3 class="text-zinc-500">no Users yet</h3>
            <%}%>
    </div>
</div>
</body>
</html>